{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([planet_labs.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'planet_labs.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
